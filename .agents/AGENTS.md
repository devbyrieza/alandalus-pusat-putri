# Workspace Rules

1. **Cross-Project Syncing**: Any change, modification, fix, or update made in the `alandalus-alimam` project MUST also be replicated, executed, and synced to the `alandalus-ululalbaab` and `template-demo` (or `ppdb-demo`) projects. Always ensure consistency across these three repositories and commit/push changes simultaneously.
